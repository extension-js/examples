{
  "private": true,
  "name": "@extension-js/content-sass-modules",
  "description": "Injects a small styled badge into every web page you visit, with an options page that turns it off.",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "sass": "^1.77.8"
  },
  "scripts": {
    "build": "extension build",
    "build:edge": "extension build --browser=edge",
    "build:firefox": "extension build --browser=firefox"
  }
}
