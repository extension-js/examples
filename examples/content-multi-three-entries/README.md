## JavaScript Content (Multiple Entries)

What you’ll see: four small boxes injected into the page corners, loaded via three separate `content_scripts` entries.

How it works here: scripts are split across three entries (2 + 1 + 1) to show how multiple entries behave in content script registration.

Build:

```bash
pnpm run build
```
