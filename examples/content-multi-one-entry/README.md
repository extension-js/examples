## JavaScript Content (Multiple Scripts)

What you’ll see: four small boxes injected into the page corners to illustrate multiple content scripts in one entry.

How it works here: a single `content_scripts` entry lists four JS files. Each script mounts a simple UI in a different corner.

Build:

```bash
pnpm run build
```
